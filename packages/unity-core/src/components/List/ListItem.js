import React from 'react';

import Typography from '../Typography';

const ListItem = ({ children }) => (<Typography type="p" component="li" noMargin>{children}</Typography>);

export default ListItem;
