import styled from '@emotion/styled';

const UnorderedList = styled.ul`
    list-style: disc;
    margin-left: 1.6rem;
    margin-top: 1.6rem;

    li {
        padding-left: 1.6rem;
    }
`;

export default UnorderedList;
