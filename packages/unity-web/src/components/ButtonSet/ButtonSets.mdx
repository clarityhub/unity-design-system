---
menu: Components
name: ButtonSets
---

import { Playground, PropsTable } from 'docz';
import Button from '../Button';
import ButtonSet, { ButtonSetPropTypes } from './ButtonSet';
import Typography from '../Typography';

# ButtonSets

A `ButtonSet` is used to group a set of action buttons together
and provide the appropriate spacing between them to make each button distinct, but
still visually grouped together.

```js
import ButtonSet from '@clarityhub/unity-web/lib/components/ButtonSet';
```

## Wrapping Buttons

<Playground>
    <Typography>Use Profile</Typography>
    <ButtonSet>
        <Button text>Back</Button>
        <Button type="primary">Edit</Button>
        <Button outline type="danger">Delete</Button>
    </ButtonSet>
    <br />
    <br />
    <Typography>Let's create a new user!</Typography>
    <ButtonSet>
        <Button>Cancel</Button>
        <Button type="primary">Create</Button>
    </ButtonSet>
    <br />
    <br />
    <Typography>Are you sure you want to delete this user?</Typography>
    <ButtonSet>
        <Button>Cancel</Button>
        <Button type="danger">Delete</Button>
    </ButtonSet>
</Playground>

## Spreading Buttons

<Playground>
    <ButtonSet spread>
        <Button text>Back</Button>
        <Button type="primary">Edit</Button>
        <Button outline type="danger">Delete</Button>
    </ButtonSet>
    <br />
    <br />
    <ButtonSet spread>
        <Button>Cancel</Button>
        <Button type="primary">Create</Button>
    </ButtonSet>
    <br />
    <br />
    <ButtonSet spread>
        <Button>Cancel</Button>
        <Button type="danger">Delete</Button>
    </ButtonSet>
</Playground>

## Prop Types

<PropsTable of={ButtonSetPropTypes} />
