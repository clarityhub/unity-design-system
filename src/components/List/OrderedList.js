import styled from '@emotion/styled';

const OrderedList = styled.ol`
    list-style: decimal;
    margin-left: 1.6rem;
    margin-top: 1.6rem;

    li {
        padding-left: 1.6rem;
    }
`;

export default OrderedList;
