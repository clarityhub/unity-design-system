import styled from '@emotion/styled';

// TODO refactor sidenav width
const SideNav = styled.div`
    box-sizing: border-box;
    padding: 3rem 2rem 3rem 0;
    width: 14rem;
`;

export default SideNav;
