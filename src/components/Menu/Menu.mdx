---
name: Menu
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { Menu, MenuItem, MenuItemDivider } from './Menu';
import Button from '../Buttons';

# Menu

## \<Menu /\> component

```js
import { Menu, MenuItem, MenuItemDivider } from '@clarityhub/one/components/Menu';
```

## Prop Types

### Menu Props

<PropsTable of={Menu} />

### MenuItem Props

All `Button` props can also be passed to the `MenuItem`.

<PropsTable of={MenuItem} />

### MenuItemDivider Props

<PropsTable of={MenuItemDivider} />



## Examples

<Playground>
    <Menu
        items={[
            <MenuItem>Item 1</MenuItem>,
            <MenuItem>Item 2</MenuItem>,
            <MenuItem>Item 3</MenuItem>,
            <MenuItemDivider />,
            <MenuItem>Long Item 4</MenuItem>,
        ]}
    >
        {({ open }) => (
            <Button onClick={open}>Open Menu</Button>
        )}
    </Menu>
</Playground>